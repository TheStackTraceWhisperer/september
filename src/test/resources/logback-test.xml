<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Disable internal status output -->
  <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

  <contextName>test</contextName>

  <!-- Root logger off for unit tests -->
  <root level="OFF"/>
</configuration>

