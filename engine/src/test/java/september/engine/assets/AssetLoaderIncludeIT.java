package september.engine.assets;

import september.engine.EngineTestHarness;

/**
 * This test verifies that the AssetLoader can correctly parse #include directives in shader files.
 */
public class AssetLoaderIncludeIT extends EngineTestHarness {
//    @Test
//    @DisplayName("AssetLoader should support the #include directive in shaders")
//    void assetLoader_supportsIncludeDirective() {
//        // Arrange: The harness provides a live GL context.
//        // We will attempt to load a shader that uses the #include directive.
//        // This test's success proves that our AssetLoader correctly processes the include
//        // and provides a valid, complete source to the shader compiler.
//
//        // Act & Assert
//        assertThatCode(() -> {
//            // Use a simple, valid fragment shader that does not need includes.
//            AssetLoader.loadShader("shaders/test_include.vert", "shaders/test.frag");
//        }).as("Loading a shader with #include should succeed because the loader supports it.")
//          .doesNotThrowAnyException();
//    }
}
